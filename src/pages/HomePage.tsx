import {FC} from 'react';
import Header from "../components/Header.tsx";

const HomePage:FC = () => {
    return (
        <div>
            <Header/>
        </div>

    );
}

export default HomePage;